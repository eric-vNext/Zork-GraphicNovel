# Handoff notes — 2026-07-11

Context for the next session working on Zork I: Graphic Novel Edition
(https://eric-vnext.github.io/Zork-GraphicNovel/, branch
`claude/zork-graphic-novel-port-65ml32`).

## Workflow: use `utils/` before every ship

Two scripts were added this session specifically so future sessions don't
re-derive the verify/deploy loop by hand:

- **`utils/verify.sh`** — runs `tsc --noEmit`, `vitest run`, and
  `npm run build` from `game/`, in the same order CI runs them. Run this
  before every commit that touches `game/src` or `game/tests`. A clean run
  here means the CI build/test job will also pass.
- **`utils/watch-deploy.sh [branch]`** — finds the latest GitHub Actions
  "Deploy to GitHub Pages" run for the branch (defaults to current branch)
  and streams it to completion via `gh run watch --exit-status`. Run this
  right after `git push` instead of manually polling `gh run list`.

Typical ship sequence:

```
./utils/verify.sh          # local typecheck + tests + build
git add -A && git commit -m "..."
git push
./utils/watch-deploy.sh    # confirm CI build+test+deploy succeeded
```

CI (`.github/workflows/deploy.yml`) runs `npm test` and `npm run build`
itself before deploying, so a broken build cannot reach GitHub Pages — but
`verify.sh` catches failures ~2 minutes sooner and without waiting on
Actions.

## Critical, durable constraint

**Do not paraphrase or invent narrative/flavor/combat text.** The user
explicitly corrected this: "I'd prefer you don't rewrite messages from the
original game text." All in-game text should be ported verbatim from the
ZIL source in `reference/zork1-source/*.zil` wherever the source has an
equivalent line. This applies codebase-wide going forward, not just to
combat.

- Combat messages are now centralized in
  [`game/src/engine/combatText.ts`](../game/src/engine/combatText.ts),
  ported verbatim from `1actions.zil`'s HERO-MELEE/TROLL-MELEE/THIEF-MELEE
  tables. Use `meleeLine()` from there for any future combat-message work
  rather than writing new lines by hand.
- **Not yet audited**: `specials.ts`, `verbs.ts`, `describe.ts`, and any
  death/resurrection text may still contain hand-written text from before
  this constraint was established. A full grep-and-compare sweep against
  `reference/zork1-source/` was not done (out of scope for last session,
  flagged here rather than done proactively). If you touch any
  player-facing string in those files, check it against source first;
  if the user asks for a general fidelity audit, budget real time for it —
  the ZIL source is large and cross-referencing takes a while.

## Testing patterns already in place

- **`game/tests/testUtils.ts`** — shared test helpers:
  - `txt(g, cmd)` — runs a command, returns joined text-event output.
  - `fightUntilWon(g, room, winFlag, attackCmd, attempts=60)` — snapshots
    state, attacks in a loop, and **restores the snapshot and retries** if
    the player dies or leaves the fight room. Combat is genuinely
    probabilistic (ported RNG-driven melee tables with a fixed seed), so a
    fixed iteration count can legitimately fail when the player dies and
    gets resurrected elsewhere mid-loop. **Always use this helper (or the
    same snapshot/retry pattern) for any test that needs to "eventually
    win a fight"** — don't assert on a bare attack loop.
- The RNG is a deterministic xorshift32 with a fixed seed (12345) per
  `Game` instance. Any change that adds/removes an `rng()` call anywhere in
  a code path shifts the entire subsequent random sequence. If tests
  suddenly start failing after an unrelated-seeming change, suspect this
  before suspecting game logic — check whether the change added or removed
  a `rng()` call count.

## Known architecture gotcha: "last panel event wins"

Multiple call sites emit a dramatic event panel (`events/...`) and then a
room-transition panel (`rooms/...`) fires afterward and silently
overwrites it, because the panel renderer just takes the last `panel`
event in a turn. This has already bitten window-entry, boat-launch,
bat-abduction, prayer-teleport, and first-entry troll combat (fixed via a
`justArrived` one-turn grace flag in `WorldState`, set in `enterRoom()` and
checked in `fightDaemon()`). **When adding any new dramatic/event panel
that fires during a move/transition, emit it after the room-transition
panel, not before** — or it will be silently discarded.

## Open item from last session (unconfirmed with user)

The user reported "I killed the troll... I look... the troll is back,"
which could not be reproduced via automated testing (TROLL-DEAD flag and
troll's null location held correctly across 200+ simulated turns and
repeated live-site tracing). The most likely explanation, now fixed, was
that the old invented stagger message ("knocking you out — no wait — the
sword connects!") read as an ambiguous/contradictory outcome and was
probably misread as the fight ending prematurely. This is deployed but
**not yet confirmed by the user** as resolving their actual experience —
if they report it again, get the exact on-screen text/image next time
rather than assuming the same root cause, since it was never technically
reproduced.

## Suggestions for next iteration

Consolidates `docs/final-build-report.md`'s original "known limitations" /
"suggested next iterations" (still valid, mostly unstarted) with new items
surfaced while fixing combat text this session. Roughly priority-ordered.

1. ✅ **Done (2026-07-11, follow-up session).** Ported the real melee
   outcome logic from `1actions.zil` — `FIGHT-STRENGTH`, `VILLAIN-STRENGTH`,
   the `DEF1`/`DEF2A`/`DEF2B`/`DEF3A-C` result tables with their exact
   `REST`-offset windows, `HERO-BLOW`/`VILLAIN-BLOW`/`WINNER-RESULT`/
   `VILLAIN-RESULT`, and `I-FIGHT`/`DO-FIGHT` — into a new `melee.ts`,
   replacing the old fixed-percentage tiers in `daemons.ts`. See
   [`game/src/engine/melee.ts`](../game/src/engine/melee.ts).
2. **Finish the verbatim-text audit** across `specials.ts`, `verbs.ts`,
   `describe.ts`, and death/resurrection text — not yet swept against source
   (see the "Critical, durable constraint" section above). The cyclops
   "shrugs but otherwise ignores your pitiful attempt" line was checked and
   not found verbatim in the portion of `CYCLOPS-FCN` read this session;
   left as-is, worth resolving properly.
3. **Build a transcript-equivalence harness** against
   `reference/zork1-source/COMPILED/zork1.z3` under dfrotz, to catch text
   drift systematically instead of via user bug reports — this would have
   caught the invented stagger-message bug automatically. Worth doing before
   further large text-touching changes, given how the last one was found.
4. ✅ **Done (2026-07-11, follow-up session).** Turned out every planned
   Tier-2 asset except the three `living-room-*` state variants was already
   generated (`docs/generated-asset-review.md`'s "not yet generated" list was
   stale). Generated the missing three as chained `nano-banana-pro/edit`
   image-to-image edits off `living-room.png` so the door, mantel, and sword
   position/angle are pixel-consistent across all four living-room states.
   Also fixed a `roomArtFor()` precedence bug where `living-room-case-full`
   could never actually show once the rug had been moved (which is required
   to reach the trap door, so effectively always). See
   [`docs/generated-asset-review.md`](generated-asset-review.md#living-room-consistency-pass-2026-07-11).
5. ✅ **Done (2026-07-11, follow-up session).** Replaced the thief's flat
   "9% chance he appears in your current room" coin flip with a new
   [`game/src/engine/thief.ts`](../game/src/engine/thief.ts) that gives him a
   real, constantly-marching position (`s.thiefRoom`) through the room list —
   he now robs any previously-visited room he's currently occupying, not just
   wherever the player happens to be standing. Also implemented `OOPS <word>`,
   `WAIT <n>` (bare `WAIT` now correctly costs 3 turns, matching V-WAIT's
   default), `SCRIPT`/`UNSCRIPT` (verbatim text + a real accumulating
   transcript via `Game.getTranscript()`), `BUG`, and tightened the Loud Room
   command gate to match `LOUD-ROOM-FCN`'s actual word dispatch (only
   SAVE/RESTORE/QUIT/WEST/EAST/UP/BUG/ECHO get through, not the looser
   ad hoc list that used to let LOOK/SCORE/every direction through). See
   `thief.ts`'s header comment for the specific scope cuts from the source's
   full `I-THIEF` (no same-turn double-pass via `<AGAIN>`; co-located
   encounters keep this project's existing flavor text rather than porting
   `THIEF-VS-ADVENTURER`'s six-way branch tree verbatim).
   **Follow-up done (2026-07-12):** added a "Transcript" button to the
   in-game HUD (`GameScreen.tsx`'s `StatusBar`) that downloads
   `game.getTranscript()` as `zork-transcript.txt` via a `Blob` + temporary
   `<a download>` click; a no-op if nothing has been scripted yet.
6. ✅ **Done (2026-07-12, follow-up session).** Added a hand-rolled service
   worker ([`game/public/sw.js`](../game/public/sw.js), registered in
   `main.tsx` behind `import.meta.env.PROD`) for offline play — no
   `vite-plugin-pwa` dependency was added, since the app has no build-time
   asset manifest to hook into. Precaches the tiny app shell (`index.html`,
   `manifest.webmanifest`) at install; everything else (art/audio, JS/CSS
   bundles) is cached opportunistically as it's actually fetched during
   play — cache-first for `/art/`+`/audio/` (content-named, effectively
   immutable), stale-while-revalidate for everything else, network-first
   for navigations. Cache writes go through `event.waitUntil()`, not a
   floating promise — a SW can be torn down the instant `respondWith`'s
   promise settles, before an un-awaited `cache.put()` finishes.
   Also added [`game/src/engine/idbSaves.ts`](../game/src/engine/idbSaves.ts)
   (3 IndexedDB-backed save slots with room/score/moves/timestamp metadata)
   and a [`SaveLoadModal`](../game/src/components/SaveLoadModal.tsx) wired
   into the title/death/play screens via a new "Save Slots" / "Slots"
   button. This is additive, not a replacement — the classic `SAVE`/`RESTORE`
   verbs still use the original single-slot localStorage path in
   `engine.ts` unchanged. Round-tripped manually (save at turn 0, take a
   turn, load slot 1, confirm state/move-count reverted).
   **Caveat**: the SW's runtime-caching `res.clone()` call could not be
   end-to-end verified inside this session's sandboxed preview browser — it
   threw `"Response body is already used"` there, but the identical
   `clone()` call succeeds on a plain page-level `fetch` in the same
   sandbox, and shell precaching (which doesn't use `clone()`) worked fine.
   This points to the preview sandbox's own network instrumentation
   consuming SW-intercepted response bodies, not a real bug — but it
   couldn't be confirmed against a real deployed site this session. If a
   future session (or the user, on the live GitHub Pages site) sees art/
   audio/JS not actually surviving into offline mode, start here.
7. ✅ **Done (2026-07-11, follow-up session).** `game/scripts/synth-audio.py`
   gained 5 region-flavored `treasure-chime-*` SFX variants (above/house/
   temple/dam/endgame — celesta-like for the house, choir-pad for the
   temple, metallic/inharmonic for the dam, etc.), remapped by current region
   at playback time in `src/state/store.ts` (`TREASURE_CHIME_BY_REGION`) so
   the engine itself stays audio-agnostic. Also added a slow, mirrored
   Ken Burns idle drift to the room panel in `GameScreen.tsx`'s `Stage`
   component — kicks in once a panel's entrance animation settles, respects
   `prefers-reduced-motion`, and is clipped safely by `.stage`'s existing
   `overflow: hidden`.

   **Follow-up bug, found by the user on the real deployed site (the
   "not independently visually verified" caveat above was warranted):**
   the fade-from-black on a new panel was taking nearly 10 seconds instead
   of the intended 0.42–0.6s. Root cause: the `settled` flag gating
   entrance-vs-idle-drift transition lived in `Stage` as a single
   `useState` shared across every panel, reset via a `useEffect` keyed on
   `panelSeq`. A new panel's first render happened *before* that effect
   fired, so its very first frame read `settled` as still `true` from the
   *previous* panel's idle loop — assigning it the 22s mirrored idle
   transition instead of the fast entrance one. Fixed by extracting the
   image into its own `PanelImage` component keyed by `panelSeq` (same key
   the `motion.img` already used), so `useState(false)` starts genuinely
   fresh per panel with no possible staleness — not just a narrower race,
   structurally impossible now. See commit around this line in git log for
   `GameScreen.tsx`.

## Repo layout notes (for orientation, not exhaustive)

- `game/` — the actual app (Vite + React + TS + Zustand + Howler).
  `game/src/engine/` is the game logic port; `game/src/data/presentation.ts`
  maps game state to art/panel keys; `game/src/parser/` is the text parser.
- `reference/zork1-source/` — original Infocom ZIL source, read-only
  reference. This is the source of truth for all game text and logic.
- `docs/asset-plan.md` — the Tier-1/Tier-2 art generation plan (already
  executed; see `docs/generated-asset-review.md` and
  `docs/final-build-report.md` for what's done).
- `game/scripts/` — Python asset pipeline scripts (image conversion, audio
  synthesis), unrelated to the `utils/` testing/deploy scripts added this
  session.
